# ============================================================
#  requirements_minimal.txt — VoiceBot Web (sans audio local)
#  Installation simplifiée pour l'interface web Flask
#  (Whisper STT via navigateur, TTS via gTTS serveur-side)
# ============================================================

# ── Backend Web ───────────────────────────────────────────────
flask>=2.3.0

# ── Embeddings + RAG (cœur du bot) ────────────────────────────
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4

# ── Modèles ML NLU ────────────────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0

# ── TTS côté serveur (voix arabe gratuite) ───────────────────
gTTS>=2.3.2
requests>=2.31.0

# ── Utilitaires ───────────────────────────────────────────────
numpy>=1.24.0
colorlog>=6.7.0
tqdm>=4.66.0
regex>=2023.10.3
